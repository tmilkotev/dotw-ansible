apiVersion: v1
kind: Pod
metadata:
  name: kube-apiserver
  namespace: kube-system
spec:
  containers:
    - name: kube-apiserver
      livenessProbe:
        failureThreshold: 8
        initialDelaySeconds: 90
        periodSeconds: 60
        timeoutSeconds: 15
      readinessProbe:
        timeoutSeconds: 15
      startupProbe:
        failureThreshold: 24
        initialDelaySeconds: 90
        periodSeconds: 60
        timeoutSeconds: 15