apiVersion: v1
kind: Pod
metadata:
  name: etcd
  namespace: kube-system
spec:
  containers:
    - name: etcd
      livenessProbe:
        failureThreshold: 8
        initialDelaySeconds: 90
        periodSeconds: 60
        timeoutSeconds: 15
      startupProbe:
        failureThreshold: 24
        initialDelaySeconds: 90
        periodSeconds: 60
        timeoutSeconds: 15