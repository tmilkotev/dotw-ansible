---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart distribution-registry
  ansible.builtin.systemd:
    name: distribution-registry
    state: restarted